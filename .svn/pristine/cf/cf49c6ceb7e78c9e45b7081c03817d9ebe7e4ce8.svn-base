function tryId(){
	swal({
		  title: '아이디 입력',
		  text: '아이디를 정확히 입력하세요.',
		  timer: 1000
		}).then(
		  function () {},
		  function (dismiss) {
		    if (dismiss === 'timer') {
		    }
		  }
		)
}

function tryPass(){
	swal({
		  title: '패스워드 입력',
		  text: '패스워드를 정확히 입력하세요.',
		  timer: 1000
		}).then(
		  function () {},
		  function (dismiss) {
		    if (dismiss === 'timer') {
		    }
		  }
		)
}

function pleaseLoginFirst(){
	swal({
		  text: '로그인이 필요한 서비스 입니다.',
		  timer: 1000
		}).then(
		  function () {},
		  function (dismiss) {
		    if (dismiss === 'timer') {
		    }
		  }
		)
}

function tryTitle(){
	swal({
		  text: '제목을 입력하세요.',
		  timer: 1000
		}).then(
		  function () {},
		  function (dismiss) {
		    if (dismiss === 'timer') {
		    }
		  }
		)
}

function tryDay(){
	swal({
		  text: '기간을 선택하세요.',
		  timer: 1000
		}).then(
		  function () {},
		  function (dismiss) {
		    if (dismiss === 'timer') {
		    }
		  }
		)
}


function tryEndDay(){
	swal({
		  text: '마감일을 선택하세요.',
		  timer: 1000
		}).then(
		  function () {},
		  function (dismiss) {
		    if (dismiss === 'timer') {
		    }
		  }
		)
}

function tryPlan(){
	swal({
		  text: '기획상태를 선택하세요.',
		  timer: 1000
		}).then(
		  function () {},
		  function (dismiss) {
		    if (dismiss === 'timer') {
		    }
		  }
		)
}

function tryTec(){
	swal({
		  text: '필요기술을 선택하세요.',
		  timer: 1000
		}).then(
		  function () {},
		  function (dismiss) {
		    if (dismiss === 'timer') {
		    }
		  }
		)
}









